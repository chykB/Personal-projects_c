#include <stdio.h>
#include <math.h>

float calculate_quarters(float i, float j);

int main()
{
    float cnt, quarter = 25;
    float result = quarter / cnt;
    printf("How much in cent are you owed? : ");
    scanf("%f", &cnt);

    calculate_quarters(cnt, quarter);
    printf("%.2f", result);
}

float calculate_quarters(float i, float j)
{
    float cent, x;
    float result = cent / x;
    return result;
}

